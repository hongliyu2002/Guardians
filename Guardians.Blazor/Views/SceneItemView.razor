@inherits ReactiveComponentBase<SceneItemViewModel>
@if (ViewModel == null)
{
    <MudText Typo="Typo.subtitle1">Loading...</MudText>
}
else
{
    <MudText Typo="Typo.subtitle1">@ViewModel.Title</MudText>
}
